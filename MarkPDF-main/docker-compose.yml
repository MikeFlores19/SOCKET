services:

  markpdf:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: markpdf-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs

    restart: unless-stopped

